$schema: http://json-schema.org/draft-04/schema#
id: acknowledgement
title: acknowledgement
type: object
namespace: https://commons.heartdata.baker.edu.au/
category: administrative
program: '*'
project: '*'
description: 'Acknowledgements for the project

  '
submittable: true
validators: null
systemProperties:
- id
- project_id
- state
- created_datetime
- updated_datetime
uniqueKeys:
- - id
- - project_id
  - submitter_id
required:
- submitter_id
- type
links:
- name: projects
  backref: acknowledgements
  label: part_of
  target_type: project
  multiplicity: many_to_one
  required: true
properties:
  $ref: _definitions.yaml#/ubiquitous_properties
  projects:
    $ref: _definitions.yaml#/to_one
  acknowledgee:
    description: Name of the individual or group to be acknowledged. Preferably as
      First name Last name
    type: string
  contact_type:
    description: The type of contact or role in the project, e.g. Principal Investigator
    $ref: _definitions.yaml#/enum_role
  orcid:
    description: The ORCID number for the acknowledgee
    type: string
    pattern: ^[0]{4}-\d{4}-\d{4}-(\d{3}X|\d{4})$
  data_release:
    description: Data release verison e.g. 0.1.1
    type: string
  data_release_date:
    description: Date of data release in the format YYYY-MM-DD
    type: string
    pattern: ^[0-9]{4}-[0-9]{2}-[0-9]{2}$
additionalProperties: false
